{
  "name": "trilha-certa-landingpage",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page estática da Trilha Certa.",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node scripts/check-dist.mjs",
    "audit:dependencies": "npm audit --omit=dev"
  },
  "dependencies": {
    "gsap": "3.13.0",
    "lenis": "1.3.11"
  },
  "devDependencies": {
    "esbuild": "^0.25.11",
    "html-minifier-terser": "^7.2.0"
  }
}
